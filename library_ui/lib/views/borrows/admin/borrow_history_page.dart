import 'package:flutter/material.dart';

class BorrowHistoryPage extends StatelessWidget {
  const BorrowHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Borrow History Page'),
    );
  }
}